# Sample data

- `sample_trace.json` — single agent execution trace (JSON object)
- `sample_traces.jsonl` — small multi-trace JSONL file for upload testing

Upload either file from the Agent X-Ray UI (**Upload**), or use the built-in Demo dataset via **Run Analysis** in Demo mode.

These samples are synthetic and contain no secrets.
